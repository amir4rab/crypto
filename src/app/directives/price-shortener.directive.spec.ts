import { PriceShortenerDirective } from './price-shortener.directive';

describe('PriceShortenerDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceShortenerDirective();
    expect(directive).toBeTruthy();
  });
});
